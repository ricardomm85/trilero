/**
 * Planner Info Components
 *
 * This module provides components for displaying and editing planner information.
 *
 * ## Components
 *
 * - `PlannerInfo` - Displays planner name and date range with edit button
 * - `EditInfoModal` - Modal for editing planner name and date range
 *
 * ## Usage
 *
 * ```tsx
 * import { PlannerInfo, EditInfoModal } from '@/components/shift-planner/planner-info';
 * ```
 */
export { default as PlannerInfo } from './PlannerInfo';
export { default as EditInfoModal } from './EditInfoModal';
